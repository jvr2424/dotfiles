common:
  - neovim
  - fzf
  - fd
  - ripgrep
  - tmux

mac:
  - bash
  - git
  - bash-completions@2
  - aerospace

ubuntu:
  - build-essential
  - fd-find
    # make an alias fd="$(which fdfind)"
    # or
    # run this command to use it like other systems
    # `ln -s $(which fdfind) ~/.local/bin/fd`
